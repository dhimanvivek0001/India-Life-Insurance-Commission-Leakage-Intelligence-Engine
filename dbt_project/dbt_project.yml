name: india_insurance_leakage
version: '1.0.0'
config-version: 2
profile: india_insurance
model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
models:
  india_insurance_leakage:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts